<h1>Blog Directory</h1>

<div class="row">
	<div class="span8">
		<ul>
		<% for p in @blogs %>
			<li>
				<h4><%= link_to p.name, blog_path(p.slug) %></h4>
				<p><%= p.description %></p>
			</li>
		<% end %>
		</ul>
	</div>
</div>