<script type="text/javascript">
  (function(doc) {
    // Resize on orientation change
      var addEvent = 'addEventListener',
          type = 'gesturestart',
          qsa = 'querySelectorAll',
          scales = [1, 1],
          meta = qsa in doc ? doc[qsa]('meta[name=viewport]') : [];

      function fix() {
          meta.content = 'width=device-width,minimum-scale=' + scales[0] + ',maximum-scale=' + scales[1];
          doc.removeEventListener(type, fix, true);
      }

      if ((meta = meta[meta.length - 1]) && addEvent in doc) {
          fix();
          scales = [.25, 1.6];
          doc[addEvent](type, fix, true);
      }

      // Hacks for IE7/8
      if (navigator.userAgent.match(/MSIE 7/)) {
        document.documentElement.className += " ie7-fix";
      } 
      if (navigator.userAgent.match(/MSIE 8/)) {
        document.documentElement.className += " ie8-fix";
      }
  }(document));
</script>

<%= stylesheet_link_tag "application", media: "all" %>
<%= stylesheet_link_tag "base/print", media: "print" %>

<%= javascript_include_tag "shared", "application" %>
<script type="text/javascript" src="http://<%= Rails.application.config.assethost.server %>/assets/client.js?bust=20130523"></script>
<script type="text/javascript" src="http://use.typekit.com/mhc7oxs.js"></script>


<script>
  try{Typekit.load();}catch(e){} // Typekit

  // Setup Nav and MegaMenu
  $(document).ready(function() {
    compactNav = new scpr.CompactNav();
    megaM = new scpr.MegaMenu({hidden:["#oas_x01"]});
  });
</script>
