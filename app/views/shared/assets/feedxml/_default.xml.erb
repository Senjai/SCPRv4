<%= assets.first.small.tag %>

<%
  caption = ''

  if assets.first.caption
    caption << assets.first.caption
  end
  
  if assets.first.owner
    caption << ";  Credit: #{assets.first.owner}"
  end
%>

<%= simple_format "<i>#{caption}</i>" %>
