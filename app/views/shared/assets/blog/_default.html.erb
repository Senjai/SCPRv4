<div class="asset-block <%= is_vertical?(assets[0].asset.eight) ? "right" : "wide" %>">
  <div class="contentasset">
    <%= assets[0].asset.eight.tag %>
  </div>

  <h4><%= assets[0].asset.owner %></h4>

  <% if assets[0].caption %>
    <p><%= assets[0].caption %></p>
  <% end %>
</div>
