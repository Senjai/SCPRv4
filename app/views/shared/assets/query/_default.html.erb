<div class="asset-block right">
  <div class="contentasset">
    <%= assets[0].asset.lsquare.tag %>
  </div>
  
  <h4><%= assets[0].asset.owner %></h4>

  <% if assets[0].caption %>
    <p><%= assets[0].caption %></p>
  <% end %>
</div>
