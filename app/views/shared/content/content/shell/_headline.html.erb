<li>
	<%= link_to content.short_headline, content.link_path %> 
	<span class="shell-link">| From <%= content.site %></span>
	<time class="smarttime" datetime="<%= content.published_at.strftime("%FT%R") %>" data-unixtime="<%= content.published_at.to_i %>"  data-window="8h"></time>
</li>