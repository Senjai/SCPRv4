<%= f.error_notification %>

<%= form_block "Details" do %>
	<%= f.input :site %>
	<%= f.input :url, input_html: { class: "long" } %>
	<%= f.input :headline, input_html: { class: "long" } %>
	<%= f.input :teaser, as: :text, input_html: { rows: "5" } %>
<% end %>

<%= section "publishing", f, record %>
<%= section "assets", f, record %>
