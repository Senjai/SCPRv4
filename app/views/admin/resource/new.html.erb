<% page_title "Add #{resource_title}" %>

<%= simple_form_for [:admin, @record], html: { class: "form-horizontal" } do |f| %>
	<%= render 'form_fields', record: @record, f: f %>
	<%= submit_row @record %>
<% end %>
