<%= form_block "Short Details" do %>
	<%= f.input :short_headline, input_html: { class: "wide" } %>
	<%= f.input :teaser, as: :text, input_html: { rows: "5" } %>
	<%= section "slug", f, record %>
	<%= extra %>
<% end %>
