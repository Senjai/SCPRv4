<% list.columns.each do |column| %>
  <td data-id="<%=record.id%>" class="column-<%=column.attribute%>" data-attribute="<%=column.attribute%>"<% if column.quick_edit? %> data-updatable="true"<% end %>>
    <%= render_attribute column, record %>
  </td>
<% end %>
