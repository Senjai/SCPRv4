<h2 id="work_status" style="text-align: center; height: 60px;">
  Working...
</h2>

<div id="spinner"></div>

<br /><hr />

<h5 style="text-align: center;">
  (You can leave this page if you want, or be redirected automatically.)
</h5>

<% content_for :footer do %>
  <script>
  // Create a room for this waiting page
  // And listen for the "Finished task" message from Newsroom
  // for this room.
    newsroom = new scpr.Newsroom(
      "<%= room_info[:id] %>", // room ID
      <%= render_json('api/private/v2/admin_users/admin_user', admin_user: current_user) %>, // user info
      { "record": <%= j record.to_json.html_safe %> }
    );
    
    listener = new scpr.Newsroom.JobListener();
  </script>
<% end %>
