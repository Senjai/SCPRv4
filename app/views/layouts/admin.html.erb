<!DOCTYPE html>
<html lang="en" xmlns:og="http://opengraphprotocol.org/schema/" xmlns:fb="http://ogp.me/ns/fb#">
<head>
  <title><%= @PAGE_TITLE ? @PAGE_TITLE + " | KPCC Admin" : "KPCC Admin" %></title>
  <%= stylesheet_link_tag "admin/admin" %>
  
  <%= javascript_include_tag "admin/admin", 
    "#{Rails.application.config.node.server}/socket.io/socket.io.js"
  %>
  
  <link rel="shortcut icon" href="http://media.scpr.org/favicon.ico" />
  <%= csrf_meta_tags %>
</head>
<body data-spy="scroll" data-target="#form-nav">
  
<div class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "KPCC Admin", admin_root_path, class: "brand" %>
      
      <% if admin_user %>
        <ul class="nav">
          <li><%= link_to "<i class='icon-home icon-white'></i> Dashboard".html_safe, admin_root_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-plus-sign icon-white"></i> Add New <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Blog Post", new_admin_blog_entry_path %></li>
              <li><%= link_to "Show Segment", new_admin_show_segment_path %></li>
              <li><%= link_to "News Story", new_admin_news_story_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Content Shell", new_admin_content_shell_path %></li>
              <li><%= link_to "Video Shell", new_admin_video_shell_path %></li>
              <li><%= link_to "Event", new_admin_event_path %></li>
            </ul>
          </li>
          <li><a href="http://a.scpr.org/a/assets" target="_new"><i class="icon-camera icon-white"></i> Launch AssetHost</a></li>
        </ul>

        <ul class="nav pull-right">
          <li><%= link_to "<i class='icon-user icon-white'></i> Profile".html_safe, admin_root_path %></li>
          <li><%= link_to "Logout", admin_logout_path, id: "logout" %></li>
        </ul>
      <% else %>
        <ul class="nav pull-right">
          <li><%= link_to "Login", admin_login_path %></li>
        </ul>
      <% end %>
    </div>
  </div>
</div>

<% if admin_user %>
  <div class="container">
    <% if breadcrumbs.present? %>
      <ul class="breadcrumb">
        <% breadcrumbs.each do |crumb| %>
          <li>
            <% if crumb.link.present? %>
              <%= link_to crumb.title, crumb.link %>
            <% else %>
              <%= crumb.title %>
            <% end %>
            
            <% unless crumb == breadcrumbs.last %>
              <span class="divider">/</span>
            <% end %>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div>
<% end %>

<div id="main" class="container">
  <div class="page-header">
    <% if content_for? :page_header %>
      <%= yield :page_header %>
    <% else %>
      <% if @PAGE_TITLE %>
        <h3><%= @PAGE_TITLE %></h3>
      <% end %>
    <% end %>
  </div>
  
  <div class="row">
    <div class="span<%= content_for?(:sidebar) ? "9" : "12" %>">
      <% flash.select { |k, v| v.is_a? String }.each do |name, msg| %>
        <div class="alert alert-<%= flash_alert_type(name) %>">
          <a class="close" data-dismiss="alert">&times;</a>
          <%= msg.html_safe %>
        </div>
      <% end %>
      
      <%= yield %>
    </div>
    <% if content_for?(:sidebar) %>
      <div class="span3" id="admin-sidebar">
        <%= yield :sidebar %>
      </div>
    <% end %>
  </div> <!-- row -->
</div> <!-- container main -->

<%= yield :footerjs %>

</body>
</html>
